node_modules/
*.tgz
.volter/tracker/tracker.sqlite
.volter/tracker/tracker.sqlite-*
.volter/tracker/tracker.sqlite.lock
.volter/tracker/local-store.json
.volter/tracker/markdown/
.volter/agent-dispatch/
.volter/keys/
.env
.env.*
.DS_Store
# Termfleet per-instance scratch files (transient tooling artifacts)
.termfleet-instance-*.json
# Build artifacts — produced by `npm run build` (and by `prepack` for npm publish);
# not committed. The GitHub Action runs ztrack from source via Bun.
visualizer/core.js
dist/

# `ztrack visualizer` auto-runs `bun install` in visualizer/ on first use, which
# regenerates this lockfile — not committed, same as the other visualizer build output above.
visualizer/bun.lock

# claude code agent worktrees (ephemeral)
.claude/worktrees/
